{
  "buildCommand": "pnpm --filter @workspace/nurul-quran run build",
  "outputDirectory": "artifacts/nurul-quran/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
